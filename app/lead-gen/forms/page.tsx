import { SectionStub } from "@/components/SectionStub";

export default function FormsPage() {
  return (
    <SectionStub
      title="Forms"
      tagline="Every form across every site — schema, submissions, conversion rate."
      bullets={[
        "List of all live forms by project",
        "Submission count and conversion rate per form",
        "Field-level drop-off analysis",
        "Form builder for new intakes",
      ]}
    />
  );
}
