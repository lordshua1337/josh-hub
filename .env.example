# Supabase (wired in P1)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# Ingestion API key (P2 — used by ~/tools/josh-hub-sync)
JH_INGEST_KEY=

# Vercel API (P3)
VERCEL_TOKEN=
VERCEL_TEAM_ID=

# GitHub API (P4) — fine-grained PAT scoped to lordshua1337
GITHUB_TOKEN=

# Anthropic API (P2 — Haiku for session summarization)
ANTHROPIC_API_KEY=

# Email — Fastmail (broadcasts + transactional + masked aliases; replaced Resend)
FASTMAIL_USER=
FASTMAIL_APP_PASSWORD=
FASTMAIL_FROM=

# Cross-project lead ingestion (vibemke → prometheus-hub /api/leads/ingest)
LEAD_INGEST_KEY=

# Auth allowlist (comma-separated emails permitted into the hub)
ALLOWED_EMAILS=

# Cloudflare R2 (P2 — raw JSONL archive)
R2_ACCOUNT_ID=
R2_ACCESS_KEY_ID=
R2_SECRET_ACCESS_KEY=
R2_BUCKET_NAME=
R2_PUBLIC_URL=
